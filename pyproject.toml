[project]
name = "pidogmcp"
version = "0.1.0"
description = "FastMCP server that wraps the pidog robot API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
    "uvicorn>=0.27.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
]