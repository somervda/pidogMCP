[project]
name = "pidogmcp"
version = "0.1.0"
description = "MCP server wrapping the Sunfounder PiDog robot API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pidog>=1.0.0",
]
